# Library Terra 开发记录

> 本文档是项目的持续开发日志和技术决策记录。以后每次修改识别流程、配置、交互或硬件方案时，应同步更新“当前状态”“测试记录”和“变更日志”。

最后更新：2026-08-29

### 2026-08-29：腾讯云首次部署

- 服务已部署到腾讯云 `81.70.232.81` 的独立 Docker 容器 `living-margins`。
- 容器仅绑定 `127.0.0.1:18780`，由现有 nginx 通过 `https://nx.ybwlawa0.com/living-margins/` 提供 HTTPS。
- 公网健康接口、网页和认证中继上报均已实际验证。
- 容器使用 `/home/ubuntu/living-margins/data` 持久化数据库、上行状态和后续 OTA 文件。
- 云端使用全新数据库；本地账号数据与既有设备令牌尚未迁移。

### 2026-08-29：公网中继第一阶段

- 新增认证的 `/api/relay/state`，Windows 识别电脑可主动向云端推送可信阅读状态。
- 新增 `cloud_relay.py`，只在 revision 改变时立即上报，并通过心跳维持在线状态。
- 云端状态超过设定时间未收到心跳时自动视为离线，避免展示陈旧页码。
- 新增 Dockerfile、`.dockerignore` 和 `.env.example`，运行数据可挂载到 `/data`。
- 新增 3 项中继认证、版本保护和云模式读取测试；全量测试增至 59 项并全部通过。
- 本机 Docker CLI 可用，但 Docker Desktop Linux 引擎未启动，镜像构建尚未执行。

### 2026-08-29：ESP32 OTA 服务回归覆盖

- OTA 引导版 0.9.0 已通过 COM7 写入，0.9.1 已生成并发布，等待无线安装后的最终实机确认。
- API v9 的固件检查和下载已覆盖升级、同版本、降级、错误令牌及无发布包场景。
- 发布清单已覆盖文件缺失、大小不符、超出 OTA 分区、SHA-256 不符和路径越界。
- 修复固件语义版本正则中的未转义小数点，`1x1x0` 等非法值不再被接受。
- 全量自动测试现为 59 项，全部通过。

## 1. 项目目标

Library Terra 希望让实体书阅读位置与数字批注系统联动：摄像头无感观察当前书页，电脑确定用户正在阅读的页面，再把对应批注发送到 ESP32-S3 触摸屏。

当前原型已经覆盖视觉定位、统一状态输出和手机网页入口：

```text
摄像头观察实体书
        ↓
识别封面并选择书籍
        ↓
确定当前页码或跨页
        ↓
输出稳定的页面状态
        ↓
HTTP 接口与虚拟 ESP32 屏幕
        ↓
手机网页：登录、绑定屏幕与阅读会话
```

OCR、页码判断和翻页检测只是实现手段。产品真正需要的是“稳定、可恢复、不打扰阅读的页面状态”。

## 2. 已确认的产品约束

- 程序运行在 Windows 电脑上。
- 最终摄像头需要固定安装，不能依赖用户手持对准。
- 书本位置可能移动，画面可能倒置或倾斜。
- 摄像头应当无感工作，不能要求用户每次翻页后按键。
- 单次识别错误不能立即改变页面或批注。
- 页码暂时无法确认时，应保留可信状态并在后台继续恢复。
- 左页、右页和装订方向不应要求用户手动选择，后续应由系统学习。
- 当前阶段接受只观察单页；只拍一页能显著增加页码所占像素。

## 3. 路线演变

### V1：固定页脚区域 OCR

最初方案把画面校准成左右两页，再在每页底部建立多个固定搜索框。每个搜索框执行多种图像预处理和 RapidOCR，最后根据“左偶右奇、左右连续”选择跨页。

主要问题：

- 书本稍微移动就离开固定区域。
- 页码很小，裁剪边界容易切掉数字。
- 正文、章节号和题号会成为高置信度假候选。
- 要求同一帧同时拍清两页，迫使摄像头离书太远。
- 一次扫描最多需要对多个裁剪框和预处理版本反复 OCR，速度慢。

### V1.5：动态纸张轮廓

程序尝试根据亮度、低饱和度和轮廓寻找书本，再把检测区域从中间分为左右页。

现场证据表明该方向不适合成为硬性识别边界：

- 66–67 的图片里，动态书本框在右侧提前结束，把清楚可见的 67 裁掉。
- 窗帘和显示器也可能满足“大片浅色区域”，被误认为书页。
- 页面弯曲、阴影和背景颜色会改变轮廓。

结论：页面位置可以作为软评分或界面提示，但不能决定 OCR 能看到哪些像素。

### V2：事件触发的整帧 OCR

程序改为对完整高清帧执行一次 OCR。运动检测负责发现翻页，画面稳定后选择一帧识别。数字候选根据位置、置信度、奇偶和同一文字行中的顺序评分。

这一版解决了关键裁剪问题：

- 66–67 原图中的页脚被识别为类似 `4 ... (2) ... 67`，系统能选择最后的 67 并推断 66–67。
- 82 页样本能识别 82，并推断 82–83。
- 83 页样本能从包含章节号的整行中选择 83。
- 没有书的窗帘画面不会输出页码。

但 V2 每次翻页只识别一次。如果该帧失败或错误，系统不会主动恢复，只能等待下一次翻页或按 `S`。

### V3：持续观察与多次一致确认

当前版本采用类似游戏循环 `Update` 的结构：摄像头每帧更新，OCR 在后台异步运行。稳定时持续处理最新帧，不建立旧帧队列。

```text
摄像头循环（高频）
    ├─ 读取最新帧
    ├─ 检测运动
    ├─ 检查亮度、对比度和清晰度
    └─ OCR 空闲时提交最新稳定帧

OCR 工作线程（低频）
    ├─ 完整帧文字识别
    ├─ 页码候选评分
    └─ 将观察结果交给页面共识器
```

页面共识器规则：

- 普通页码必须连续出现 2 次才确认。
- 相对当前状态跳动超过 `max_page_jump` 时，需要连续出现 3 次。
- 单次无结果不清空当前页。
- 单次不同结果只成为待确认候选，不会立即显示。
- OCR 工作线程完成后获取当时最新稳定帧，不积压旧帧。
- 已确认后仍持续低频复查，以便自动纠错。
- 黑屏、遮挡、对比度不足和严重模糊帧不会进入 OCR。

## 4. 当前架构

主要文件：

- `app.py`：摄像头循环、运动状态、后台 OCR 调度、页面共识、调试记录和键盘交互。
- `library_terra/vision.py`：运动检测、OCR 数字候选、候选空间评分和跨页推断。
- `library_terra/books.py`：已注册书籍、封面视觉匹配和书籍共识。
- `library_terra/reading_state.py`：统一阅读状态、原子快照、语义去重、HTTP 接口和设备反馈。
- `library_terra/web_database.py`：网页用户、角色、持久登录、屏幕绑定、阅读会话和批注生命周期的 SQLite 数据层。
- `virtual_screen.py`：通过 HTTP 独立读取状态的 480×320 虚拟 ESP32 屏幕。
- `living_margins_web.py`：无额外依赖的手机网页服务器与 JSON 接口。
- `web/`：登录、主页、屏幕绑定和阅读状态的移动端界面。
- `config.json`：摄像头、画面过滤、识别频率和确认次数配置。
- `runtime/`：当前阅读状态、语义事件历史和设备反馈。
- `debug/live/`：最新一次自动识别的完整画面与候选结果。
- `debug/时间戳/`：按 `S` 重新锚定时保留的独立记录。

当前状态流：

```text
TURNING
    翻页或明显运动，暂停提交 OCR
        ↓
VERIFYING
    画面稳定，持续取得最新 OCR 观察
        ↓ 两次一致
STABLE
    页面已确认，仍每隔约 1 秒允许后台复查
```

`UNCONFIRMED` 表示尚无可信页面。程序会继续后台识别，不需要用户立即干预。

识别主循环只在有产品意义的节点发布状态：书籍确认、翻页开始/结束、页码确认、人工修正和程序停止。每次有效变化都有单调递增的 `revision`；普通 OCR 观察和重复状态不会推送。翻页过程中保留最后确认页，直到新页通过共识，避免显示端闪空或提前跳到错误批注。

## 5. 当前交互

- `S`：清除旧锚点，立即复制当前有效帧并开始重新验证。仍需两次一致，避免单帧标题数字成为页码。
- `A`：手动回退一个跨页。
- `D`：手动前进一个跨页。
- `Q` / `Esc`：退出。

这些按键是开发和恢复工具。最终产品中，摄像头应自动工作，ESP32-S3 触摸屏只保留低频恢复入口。

手机网页当前交互：

- 用户名和密码注册/登录，登录令牌默认保留 30 天。
- 屏幕绑定与阅读会话相互独立；结束阅读不会解除屏幕绑定。
- Demo 虚拟屏幕机器码为 `LM-DEMO-0001`。
- 阅读会话可开始、暂停、继续和结束；进行中会同步桌面 OCR 的最新可信书籍与页码。
- 暂停后停止同步视觉状态，冻结当时的书籍、页码和状态版本，为下一阶段“暂停并写本页批注”提供稳定上下文。
- 暂停成功后进入批注编辑页；批注可以保存为私有草稿，也可以提交为待审核状态。
- 主页“我的批注”列出草稿和审核状态，草稿可继续编辑，已提交内容保持只读。
- 本地最早创建的账号作为 Demo 管理员；管理员主页包含待审核队列，可以批准或拒绝批注。
- 批准后的跨页批注写入对应书籍的 `comments.json`，现有识别程序与虚拟屏幕无需新协议即可在约 1 秒内读取。
- “此处有灵感”在阅读中只做一次持久标记，不暂停阅读、不弹出编辑器；首页灵感夹可在阅读后将其转为冻结上下文的批注草稿。
- 当前手机网页还没有直接调用手机摄像头，只接受正在运行的桌面 OCR HTTP 服务返回的实时状态；`runtime/state.json` 只保留作诊断快照，不再驱动网页显示。手机浏览器摄像头与 HTTPS/云部署保留在后续阶段。

## 6. 现场测试记录

### 2026-08-24：电脑摄像头双页测试

- 分辨率：1920×1080。
- 两页同时入镜后页码占用像素较少，焦点不稳定。
- 固定裁剪 OCR 经常把正文数字组合为错误跨页。
- 整帧 OCR 实际能够在样本中识别 82、83。

### 2026-08-25：66–67 失败复盘

- 页面 67 肉眼清楚可见。
- 完整帧 RapidOCR 能识别包含 67 的页脚行，置信度约 0.837。
- 动态书本框提前结束，局部 OCR 没有看到 67。
- 窗帘场景曾被纸张轮廓算法误判为书。
- 错误的 P30–P31 曾通过最大跳页限制阻止系统恢复到 66–67。
- V2 整帧方案回归后正确输出 66–67。

### 2026-08-25：手机摄像头与单页测试

- 手机作为 Windows 虚拟摄像头连接成功。
- 程序请求 1920×1080，但实际收到 1280×720。
- 单页近距离画质明显好于电脑摄像头双页画面，识别准确率显著提高。
- 距离继续增加后页码像素不足，准确率下降；该问题暂时后置。
- 自动识别曾将真实 75 误读为 175，并输出 174–175。
- 一张真实 84–85 的画面中，页脚 85 未被检测，标题 56 被误认为页码并输出 56–57。
- 最新黑屏帧能够被 V3 画面有效性过滤拒绝。

### V3 共识回归

使用真实手机测试帧模拟连续观察：

```text
56–57（单次错误）
84–85（第一次）
84–85（第二次）
```

结果：56–57 未显示，第二个 84–85 到达后才确认。

另一序列：

```text
当前确认 84–85
174–175（单次错误）
84–85（恢复）
86–87（第一次）
86–87（第二次）
```

结果：174–175 没有覆盖当前页；86–87 连续出现两次后才更新。

### 2026-08-26：V3.1 首次连续会话

会话 `debug/sessions/20260826-044545/` 持续约 396 秒：

- OCR 145 次，其中 73 次产生页码观察，72 次无结果，OCR 无结果率约 49.7%。
- 自动确认页面 11 次，未发生 OCR 程序异常。
- OCR 平均耗时约 1.25 秒。
- 有记录的页面确认平均延迟约 6.55 秒，未达到 3 秒目标。
- 运动检测记录了 85 次场景变化；相邻场景事件中 42 次间隔小于 0.25 秒，证明单帧阈值在临界值附近反复抖动。
- 本次没有使用 `Y/N/M`，因此不能从日志计算真实准确率；只能确认系统确实连续输出了页面序列。

结论：下一瓶颈首先是运动事件抖动和稳定帧质量，而不是左右页学习。

### 2026-08-26：V3.2 正常翻页与大跳页测试

会话 `debug/sessions/20260826-045712/` 持续约 205 秒：

- 运动场景事件从上一轮的 85 次下降到 7 次，过期 OCR 从 30 次下降到 5 次，说明运动迟滞生效。
- 正常逐页翻阅能够连续确认 62–63、64–65、68–69，随后一直跟踪到 90–91，主观体验可用。
- 从 90–91 大跨度翻到 110–111 后，约 48 秒持续返回无结果；按 `S` 重新锚定后才确认 110–111。
- 失败期间 OCR 几乎每次都识别到了 111，且 110–111 是候选第一名，但旧页连续性扣分把排名从约 4.3 压到约 3.1，低于 3.75 输出阈值。

结论：这是候选层与共识层对大跳页重复惩罚造成的状态锁定，不是摄像头或 OCR 看不见页码。

### 2026-08-26：V3.3 连续大跳页测试

会话 `debug/sessions/20260826-050625/` 测试了 110→74 以及后续向约 30 页区域的大跳：

- 110→74 最终成功，但确认较慢。
- 后一次大跳持续失败；145 次 OCR 中只有 11 次产生观察，观察率约 7.6%。
- 自动重新捕获实际运行了 113 次，证明恢复调度已经启动但候选几何过滤仍在拒绝结果。
- 最后保存的完整画面中，当前可见顶层页面清晰显示 32，左下还露出底层纸页的 16。
- OCR 对 32 和 16 的置信度都接近 1.0，但原算法使用视频画面高度判断“是否靠近页脚”。书页在画面约 y=588 结束，数字 32 在 y=462–483；它相对书页是页脚，相对 720 高的视频却不够靠下，因而没有进入排名。

结论：V3.3 解决了旧状态锁定，但没有解决书本在画面中移动后页脚坐标失真；需要页面相对几何，而且必须处理底层纸页露出的第二个页码。

### 2026-08-26：首本真实书籍录入与离线封面验收

通过 V4.1 拍照审查流程录入：

- 书籍 ID：`try-n1-060455`
- 书名：《TRY-新日语能力考试N1-语法必备》
- 裁正封面尺寸：483×619
- OCR 对“新日语能力考试”和“语法必备”的置信度分别约为 99.1% 和 99.8%，人工修正后入库。
- 封面提取到 1625 个 ORB 特征点。

使用该真实封面离线构造不同摄像头视角：

- 居中缩放：823 个有效匹配，800 个 RANSAC 透视内点，成功。
- 明显透视倾斜：523 个有效匹配，500 个内点，成功。
- 透视倾斜并增加大面积遮挡：520 个有效匹配，494 个内点，成功。
- 66–67 内页、82–83 内页和窗帘画面均没有误匹配成该书。

当前确认阈值只要求至少 16 个有效匹配、10 个透视内点和 38% 内点率；真实封面的余量充足，无需降低阈值。仍需用主程序重新拍摄一次不同姿态的真实封面，验证不是只匹配录入照片本身。

### 2026-08-26：真实封面现场匹配通过

会话 `debug/sessions/20260826-060751/` 使用主程序识别已注册的 `try-n1-060455`：

- 第一次有效匹配发生在会话约 20.02 秒：69 个有效特征匹配、45 个透视内点、内点率 65.2%，进入书籍待确认 1/2。
- 第二次发生在约 21.25 秒：193 个有效匹配、168 个内点、内点率 87.0%，确认《TRY-新日语能力考试N1-语法必备》。
- 从第一次候选到正式确认约 1.24 秒。
- 本次共执行 7 次封面扫描，7 次均匹配同一本书，完成 1 次书籍确认，没有封面匹配异常。
- 后续稳定帧仍保持 150～274 个透视内点，实际摄像头姿态下余量充足。

结论：本地封面注册、实际摄像头封面匹配和两次一致确认已经通过。该会话在封面仍处于画面时结束，没有出现确认书籍后的页码确认事件；“换书后清空旧页并重新确认内页”的组合流程仍需在后续联调中覆盖。

### 2026-08-26：V5 虚拟屏幕端到端验收通过

用户实际运行 `run.bat` 与 `virtual_screen.bat` 后确认显示效果良好。对应会话为 `debug/sessions/20260826-061922`，持续约 90.2 秒：

- 21 次封面扫描中有 6 次匹配，最终确认书籍 `try-n1-060455` 一次。
- 46 次页码 OCR 中有 42 次产生观察、4 次无结果、0 次异常，产生 7 次页码确认。
- 状态历史从 revision 1 连续增长到 17，依次记录启动、书籍确认、页码确认和翻页状态。
- 虚拟设备先收到书籍确认，随后收到 28–29、34–35、36–37、58–59、62–63、32–33、184–185 等稳定页面事件。
- 翻页开始和识别期间仍携带此前可信页码，例如 36–37 和 62–63；只有新页确认后才替换，符合防闪烁设计。
- 本次没有 `Y/N/M` 人工正确性标记，因此日志不能独立证明上述每次页码都正确；正确性结论只采用用户的现场观察。
- 本次没有生成 `runtime/feedback.jsonl`，说明赞同/不赞同按钮链路尚未进行现场点击验收；HTTP 自动化测试已通过。

结论：封面识别、页码确认、统一状态、HTTP 轮询和虚拟屏幕显示已经形成可用的现场闭环。接下来可以进入 `(book_id, page)` 批注数据与选择逻辑，不再继续扩张视觉模块范围。

## 7. 当前配置基线

关键参数：

```json
{
  "motion_threshold": 9.0,
  "settle_seconds": 0.8,
  "confirmations_required": 2,
  "large_jump_confirmations": 3,
  "ocr_retry_interval_seconds": 0.25,
  "ocr_verified_interval_seconds": 1.0,
  "min_frame_brightness": 18.0,
  "min_frame_contrast": 8.0,
  "min_sharpness": 12.0,
  "max_page_jump": 12
}
```

一次 OCR 如果超过上述间隔，程序不会并发启动更多任务，而是在当前任务完成后获取最新帧。

## 8. 已知问题与未决决策

### 相机距离与分辨率

手机虚拟摄像头当前实际只有 1280×720。单页近距离可用，继续拉远后页码像素不足。后续应使用手机本地 4K 视频判断问题是否来自虚拟摄像头降采样，再决定是否购买摄像头。

### 左右页与装订方向

当前候选评分仍使用画面位置和页码奇偶推断跨页，这不应成为最终产品中的用户配置。

拟议方向：

1. 先保存单页 `observed_page`，不急于确定左右关系。
2. 连续观察同一物理侧的页码序列，例如 85、87、89。
3. 结合页码在页面边缘的位置、文字方向和增减规律，自动学习摄像头观察侧与装订方向。
4. 学习完成前只使用可见页码，不能要求用户选择左页或右页。

该方案尚未实现，需要在下一阶段讨论并验证。

V5.1 的批注层会在当前跨页内选择一条批注，并在屏幕上标明其实际页码。如果两页都有批注，使用人工优先级和页码顺序确定性选择；这保证系统行为稳定，但不等于已经解决单页左右判断。该限制不能在后续产品验收中被忽略。

### 持续 OCR 的资源占用

V3 会在稳定时持续识别。当前通过单工作线程、最新帧优先和运动暂停避免任务堆积。后续需要实测 CPU 占用、平均识别周期和手机发热。

### 确定性假候选

多帧一致可以过滤偶发错误，但如果 OCR 在相同构图中持续把同一个标题识别成页码，两次一致仍可能错误。后续可能需要：

- 使用真实页面边界而不是画面边界计算页脚位置；
- 学习特定书籍的页脚版式；
- 保存可见页码序列并加强连续性约束；
- 在已知电子文本时使用正文指纹匹配作为补充信号。

## 9. 下一步建议

### 页码阶段收口状态：V3.4 页面相对页脚识别

V3.1 已让持续观察流程可测量，V3.2 修复了运动抖动和清晰帧选择，V3.3 解除了大跳页的旧状态锁定。第三次会话证明书本位置变化后，固定使用视频边界判断页脚仍会造成长时间失败。V3.4 改用页面相对软几何。

V3.4 复测要求：

1. 从已确认页面连续进行两次方向或跨度不同的大跳页，全程不按 `S`。
2. 正确大跳页应在三次一致观察后自动替换旧页，目标是画面稳定后 5 秒内恢复。
3. 日志中应出现正常的 `pending 1/3`、`2/3`、`confirmed`，必要时可看到 `mode=reacquire`。
4. 对同一标题数字的单次或两次错误观察仍不能改变已确认页面。
5. 移动书本上下位置后仍能确认，因为页脚位置应相对检测到的纸张范围计算。
6. 大跳页复测通过后，再进入单页 `observed_page` 与左右页自动学习。

验收标准：

- 连续翻阅至少 20 次，全程不按 `S`。
- 正确页面能在画面稳定后 3 秒内自动确认。
- 单次错误候选不会覆盖已确认页面。
- 识别失败后无需再次翻页，能通过后台复查自动恢复。
- 测试结束后能从日志计算正确确认率、错误切换次数、漏检次数和平均恢复时间。

完成 V3.4 复测后，根据数据选择下一步：

- 如果页码经常根本没有进入 OCR 结果：优先处理相机画质、安装距离和局部高清采样。
- 如果正确页码已进入 OCR、但被错误数字击败：改进候选评分和页脚/页面边界模型。
- 如果观察值正确、但状态更新太慢：调整 OCR 调度和共识策略。
- 只有持续识别达到可用基线后，才实现单页 `observed_page` 与左右页/装订方向自动学习。
- ESP32 通信放在页面状态稳定之后；它只消费结果，不应参与视觉识别。

### 已进入下一阶段：V4 本地封面匹配

页码 OCR 暂时收口后，新增封面识别以确定“当前是哪一本书”。当前选择本地注册方案，而不是只依赖封面文字 OCR：

1. 每本书先注册一张清晰参考封面和书籍 ID/标题。
2. 稳定画面在后台提取 ORB 局部特征，与本地封面逐一匹配。
3. 使用 Lowe ratio test 过滤模糊对应，再用 RANSAC 单应性验证这些特征是否来自同一个平面封面。
4. 封面需要连续匹配两次才切换当前书籍。
5. 切换书籍后清除旧书的页码状态；封面离开画面后恢复页码 OCR。

这一阶段先解决有限个人书库或比赛演示中的可靠匹配。互联网任意书籍搜索、ISBN 元数据和后端书库同步不在当前实现范围内。

### 已进入下一阶段：V5 统一阅读状态与虚拟设备

ESP32 尚未到货，因此先冻结设备通信契约，而不是等待硬件：

1. 视觉模块把书籍、可信页码、状态、来源和版本号写入统一状态。
2. 状态快照使用同目录临时文件替换，显示端不会读到半写入 JSON。
3. 只有语义变化才增加版本号和事件历史，后台 OCR 帧不会造成设备刷新风暴。
4. 本机 HTTP 接口提供 `GET /state`，未来 ESP32 和当前虚拟屏幕使用同一协议。
5. `POST /feedback` 接收赞同/不赞同，并把当时的书籍、页码和状态版本一同记录。
6. 电脑虚拟屏幕模拟 480×320 设备，每 0.5 秒读取一次接口，验证断线重试、状态显示和反馈。

V5.1 已建立 `(book_id, page)` 批注数据层并把内容加入设备消息。下一步应先进行真实批注现场验收，再决定开发批注生成/同步，还是回到单页 `observed_page` 与左右侧自动判断。

### 已进入下一阶段：V6 手机网页主入口

第一条纵向闭环已经实现：

```text
注册或登录
    → 使用机器码绑定页边屏幕
    → 开始阅读会话
    → 跟随现有 OCR 书籍与页码
    → 暂停时冻结页面上下文
    → 继续或结束阅读
```

产品决策：

1. 屏幕属于用户的持久设备，阅读会话只是一次阅读过程，二者不能共用生命周期。
2. 黑客松 Demo 使用简单用户名和密码，不实现邮箱验证、找回密码和多因素认证。
3. “暂停并写批注”是正式状态切换，不能当成普通页面跳转；暂停时冻结 `book_id`、页码和状态版本。
4. “此处有灵感”以后直接记录当时最后可信页码，不再额外设计页码不稳定流程。
5. 批注生命周期采用 `draft → pending → approved/rejected`；只有 `approved` 可以进入页边屏幕候选。
6. 反馈最终由数据库保证同一用户对同一批注只有一条当前选择，替代现在追加式日志可能出现的重复反馈。
7. 云部署与 HTTPS 已列入正式范围但优先级较低；开始使用手机浏览器直接拍摄时，HTTPS 将从可选项变成前置条件。

下一开发顺序：

1. 已完成：在冻结的阅读上下文中实现手机批注编辑、草稿保存和提交审核。
2. 已完成：管理员审核入口，以及批准后同步到现有书籍批注与页边屏幕链路。
3. 已完成：“此处有灵感”即时标记，以及阅读结束后的待补写列表和转草稿流程。
4. 已完成：把赞同/不赞同接入登录用户，增加 `(user_id, comment_id)` 唯一约束、改票与状态恢复。
5. ESP32 到货后实现设备令牌、二维码配对与真实屏幕联调。
6. 最后进行 HTTPS 云部署，并把手机浏览器摄像头接入阅读状态。

## 10. 变更日志

### 2026-08-28 — V6.7 ESP32 实机与设备令牌

- Waveshare ESP32-S3-Touch-LCD-4.3 已完成屏幕、触控、中文字体、Wi-Fi、状态轮询和批注反馈实机联调。
- 修复反馈按钮在状态自动刷新后丢失“已赞同/已不赞同”显示，以及识别中仍有批注却错误禁用按钮的问题。
- 设备表新增令牌哈希；令牌可重新签发，旧令牌随即失效，数据库不保存原始凭据。
- ESP32 的当前反馈与提交反馈请求同时携带机器码和设备令牌；只知道机器码不再能够冒充屏幕。
- 设备接口对无效令牌返回 HTTP 401；正确令牌的首次反馈、重复反馈和改票语义保持不变。
- API 升级至 v4，并声明 `device_tokens` 能力；本机令牌仅保存在 Git 忽略的固件密钥文件中。
- 38 项完整自动化测试及非法令牌专项测试通过；实机固件编译、写入与校验通过。
- 下一步进入二维码配对，避免最终用户手工输入机器码或接触设备令牌。
### 2026-08-28 — V6.6 登录用户反馈与改票

- 新增持久化 `comment_feedback` 表，以 `(user_id, comment_id)` 唯一约束保证同一用户对同一批注只有一条当前选择。
- 虚拟屏幕不再把按钮结果只追加到匿名 JSONL；它使用机器码找到屏幕绑定用户，并把反馈写入网页数据库。
- 首次选择返回 `created`，重复同一选择返回 `unchanged` 且不新增记录，切换选择返回 `changed` 并更新原记录。
- 虚拟屏幕显示勾选状态；重启屏幕、翻走后再次回到批注，都会查询并恢复当前绑定用户的选择。
- 翻页和网络请求并发时，异步反馈结果会校验批注 ID，上一页的结果不会错误显示到新批注上。
- 未绑定屏幕不能提交或查询用户反馈；服务端负责设备归属和反馈值校验。
- 网页能力握手升级到 API V3，并公布 `user_feedback` 能力。
- 当前本地 Demo 仍以机器码作为设备身份；云部署前必须升级为不可猜测且可撤销的设备令牌。
- 新增设备绑定、唯一性、重复点击、改票、恢复查询与 HTTP 端到端测试；全项目 38 项测试通过。

### 2026-08-28 — V6.5 灵感夹与阅读后补写

- 修复本地新旧网页服务同时存在时，新前端请求旧后端产生灵感接口 404 的问题；服务已清理为单实例并完成接口实测。
- 网页握手新增 API 版本与能力列表；前端只有确认后端支持 `inspirations` 才启用按钮，旧服务会明确提示需要重启而不是点击后报“接口不存在”。
- 阅读页“此处有灵感”正式启用：只在存在活跃阅读会话和可信书籍/跨页时可用。
- 标记动作不会暂停阅读、不会打开输入框，也不会改变 OCR 跟随状态，操作完成后仅给出短暂反馈。
- 同一用户对同一本书同一跨页只能保留一条未补写标记，连续误触不会制造重复记录。
- 新增持久化灵感夹，保存用户、阅读会话、书籍、冻结跨页、状态版本和创建时间。
- 首页灵感夹列出全部待补写书页；点开后原子生成批注草稿并把标记改为 `converted`。
- 灵感生成的批注草稿不依赖原阅读会话仍处于暂停状态，阅读结束后仍可补写、保存和提交审核。
- 灵感与批注所有权均由服务端验证，其他用户不能转换不属于自己的标记。
- 新增灵感防重复索引和灵感到批注的一对一索引；数据库初始化会自动迁移现有本地数据。
- 新增标记不中断阅读、防重复、冻结上下文、所有权、转草稿与 HTTP 端到端测试；全项目 37 项测试通过。

### 2026-08-28 — V6.4 管理员审核与页边屏幕发布闭环

- 人工验收已通过：手机提交、管理员审核及批准后的显示链路在实际运行中符合预期。
- 用户增加 `admin/reader` 角色；本地 Demo 中最早创建的账号自动成为管理员，后续账号默认是普通读者。
- 角色与审核权限在服务端强制校验，普通读者即使直接请求接口也无法查看队列或审核批注。
- 管理员主页新增“审核队列”，可以查看提交者、书籍、冻结跨页和正文，并执行批准或拒绝。
- 批准前先用稳定 ID 将批注写入对应书籍的 `comments.json`；写入失败时保持待审核，避免数据库显示已发布但屏幕没有内容。
- 批准的批注保留 `page_start–page_end` 范围；当前跨页与该范围有交集时即可进入显示候选，不要求用户手动判断左右页。
- 虚拟屏幕显示跨页署名信息；现有识别主程序每秒重载书籍批注，因此无需重启即可看到新批准内容。
- 新增待审核索引，队列按提交时间稳定排序；同一批注重试发布不会在书籍文件中产生重复记录。
- 新增角色、越权、审核、跨页匹配、发布文件与 HTTP 端到端测试；全项目 35 项测试通过。
- “首个账号即管理员”是黑客松本地演示策略，云部署前必须替换为显式管理员初始化。

### 2026-08-28 — V6.3 手机批注草稿与提交审核

- 新增独立 `web_comments` 表，保存作者、阅读会话、书籍、冻结跨页、正文、生命周期和时间字段。
- 批注生命周期正式启用 `draft → pending → approved/rejected`，本阶段实现前两个状态。
- 新建批注必须来自已暂停的阅读会话；暂停前后端会重新读取实时 OCR，未确认书籍或页码时拒绝进入编辑，避免给历史页面写批注。
- 冻结上下文保存当前跨页范围，不要求用户手动选择左页或右页；精确单页归属继续等待自动单页判断。
- 新增手机批注编辑页，支持 2000 字计数、保存草稿、提交审核、返回阅读和状态说明。
- 主页新增“我的批注”列表，显示书名、跨页、内容摘要和草稿/待审核状态；草稿可再次打开编辑，待审核内容保持只读。
- 每个阅读会话最多保留一条未提交草稿；重复保存会更新原草稿，不产生重复记录。
- 所有保存和提交操作都在服务端校验登录用户所有权，其他用户无法读取或修改批注。
- 新增批注上下文、草稿更新、提交、防重复、所有权和 HTTP 端到端测试；全项目 31 项测试通过。

### 2026-08-28 — V6.2 实时 OCR 在线语义

- 修复 OCR 程序未运行时，网页仍把上次 `runtime/state.json` 中的书籍与页码显示为当前识别的问题。
- 手机网页不再直接读取磁盘快照，而是以短超时请求 `http://127.0.0.1:8765/state`。
- 只有本次正在运行的 `ReadingStatePublisher` 返回 HTTP 200 时，状态才具备“实时”资格；连接失败、HTTP 503、无效 JSON 和超大响应全部视为离线。
- 活跃阅读在离线时清空书名与页码，并显示“识别服务 OFFLINE / OCR 未运行，不显示历史页码”。
- 暂停阅读仍显示数据库中主动冻结的书籍和页码，保证后续写批注的上下文不会因为 OCR 离线而丢失。
- 新增实时服务、服务离线和服务未就绪三类测试；全项目 28 项测试通过。

### 2026-08-28 — V6.1 StudioBlank UI Rule 与前端完全重构

- 新增 `DESIGN_RULES.md`，把 StudioBlank 颜色、字体、间距、组件和禁止项固化为项目强制 Rule。
- 完全删除 V6.0 的纸张纹理、绿色/金色主题、圆形大字、轨道装饰、圆角卡片、阴影、渐变和模拟书页图形。
- 登录页改为价值主张与账号表单的严格双栏，移动端自动变成上下结构。
- 首页改为高留白的单主操作布局；屏幕和灵感使用编号、水平线和等宽状态文本。
- 阅读页让页码成为唯一视觉主角，书名、识别状态、设备和操作退回清晰的信息层级。
- 绑定屏幕由圆角底部面板改为直角黑线弹窗，并补充遮罩点击、Escape 和移动端关闭入口。
- 全站颜色收敛为 `#0A0A0A`、`#FAFAFA`、白色和中性灰；成功与错误色只用于真实状态。
- 现有登录、绑定、阅读会话和 OCR 状态逻辑保持不变。

### 2026-08-28 — V6.0 手机网页、持久登录与阅读会话

- 新增移动端主网页，包含登录/注册、阅读主页、屏幕绑定底部面板和阅读状态界面。
- 使用 Python 标准库 HTTP 服务与 SQLite，不新增部署依赖；可通过 `run_web.bat` 启动。
- 密码使用随机盐和 `scrypt` 保存，登录令牌只以哈希形式持久化，并通过 HttpOnly、SameSite=Lax Cookie 保持 30 天登录状态。
- 新增持久设备表；Demo 自动注册机器码 `LM-DEMO-0001`，同一屏幕不能同时绑定给两个用户。
- 新增独立阅读会话，可开始、暂停、恢复和结束；新会话自动结束同一用户此前未结束的会话。
- 活跃会话同步现有视觉状态；暂停会话不再接受同步，从而冻结写批注所需的书籍、页码和 revision。
- 网页直接读取现有 `runtime/state.json`，没有修改 OCR、统一状态接口或虚拟 ESP32 屏幕协议。
- 新增数据库与 HTTP 契约测试；全项目 25 项测试通过。

### 2026-08-26 — V5 统一阅读状态与虚拟 ESP32 屏幕

- 新增 `ReadingStatePublisher`，输出含书籍、跨页、状态、来源、事件、更新时间和单调版本号的稳定 JSON 契约。
- 状态快照采用写临时文件后原子替换；追加式 `events.jsonl` 只记录语义变化并自动去重。
- 封面确认、翻页开始/结束、页码确认、人工重新锚定与修正全部接入统一状态层。
- 翻页期间保留最后可信页码，新页只有通过原有两次/三次共识后才替换。
- 新增无第三方依赖的 HTTP 服务：`GET /state`、`GET /health` 和 `POST /feedback`。
- 新增 `virtual_screen.py` 与 `virtual_screen.bat`，在 ESP32 到货前模拟 480×320 显示、网络轮询和赞同/不赞同交互。
- 状态、历史与反馈分别保存在 `runtime/state.json`、`runtime/events.jsonl` 和 `runtime/feedback.jsonl`。
- 新增状态去重、跨重启版本递增、HTTP 状态读取和反馈归属测试；全项目 17 项测试通过。

### 2026-08-26 — V5.1 页码批注数据层

- 新增 `library_terra/comments.py`，每本书使用独立 `comments.json` 保存页码、内容、署名、优先级和启用状态。
- 跨页内批注按优先级、页码顺序和 ID 确定性选择，重复运行不会随机换文案。
- 统一阅读状态新增 `comment` 对象；批注内容发生变化时才增加版本号，普通定时检查会被去重。
- 主程序在稳定页面下每秒重新读取批注，因此编辑后无需重启识别程序。
- 反馈记录新增 `comment_id`，可以确认赞同/不赞同针对的是哪条批注，而不只记录当时页码。
- 虚拟屏幕开始显示真实批注、署名和目标页；没有批注时明确显示“本页暂时没有批注”，且反馈按钮保持禁用。
- 新增 `comment_editor.py` 与 `add_comment.bat`，提供书籍选择、页码、署名、优先级、启用、修改和删除的图形化管理界面。
- 修复翻页后 OCR 仍确认原页时外部状态可能停在 `recognizing` 的问题；同页复核成功后重新发布 `stable`。
- 新增批注增删改、跨页选择、停用/损坏数据过滤及反馈绑定测试；全项目 19 项测试通过。

### 2026-08-26 — V4 本地封面匹配

- 新增 `library_terra/books.py`，使用 ORB 特征、比率检验、RANSAC 透视内点和投影面积匹配注册封面。
- 新增两次一致的书籍共识器，单帧封面误匹配不会切换当前书籍。
- 封面匹配运行在独立后台线程，仅在无页码、连续页码无结果或刚发生运动时搜索，不阻塞摄像头预览。
- 确认新书后自动清除旧书页码状态；封面仍在画面时短暂暂停页码 OCR，避免封面数字成为页码。
- 新增 `manage_books.py` 和 `add_book.bat`，用于注册参考封面和查看本地书库。
- 会话日志新增封面扫描、特征数、透视内点、面积、待确认与书籍确认事件。
- 新增旋转透视封面匹配、普通内页防误判和书籍共识测试；全项目 12 项测试通过。

### 2026-08-26 — V4.1 拍照、OCR 与人工审查录入

- `add_book.bat` 从逐项终端输入改为摄像头拍照录入，不再要求用户准备图片路径或手工填写内部 ID。
- 拍照画面自动寻找最大合理四边形封面并显示绿色轮廓；检测失败时提供黄色中央取景框。
- 捕获后执行透视裁正，只保存封面本身，避免把桌面背景特征录入匹配库。
- 使用 RapidOCR 读取封面所有文字，根据相对字号、宽度、位置和置信度生成可编辑标题建议。
- 同时尝试正向和旋转 180 度 OCR，选择整体文字质量更高的方向。
- Tkinter 审查窗口显示封面、建议书名、全部 OCR 文字与置信度；用户可以确认、修改书名、重新拍照或取消。
- 内部书籍 ID 根据最终书名和时间自动生成并处理重复；用户无需编辑。
- `book.json` 记录人工审查状态、录入来源、时间以及 OCR 原始结果。
- 保留 `manage_books.py add` 作为已有图片和替换封面的高级备用入口。
- 新增封面四边定位与裁正、标题排序和中英文/非拉丁标题 ID 测试；全项目 15 项测试通过。

### 2026-08-26 — V3.4 页面相对软几何

- 根据连续大跳会话定位到 32 已被高置信度 OCR 识别，但因书页没有贴近视频底边而被页脚几何过滤。
- 完整画面 OCR 保持不变；动态纸张范围只用于归一化页码位置，不再作为任何硬裁剪边界。
- 若纸张范围低估并把数字放在范围外，候选自动回退到完整画面坐标，不会重现 V1.5 裁掉 67 的问题。
- 对纸张极端外沿露出的底层页码进行软降权，当前画面中顶层页码 32 可以压过底层页码 16。
- 调试结果和会话日志新增 `page_box`，每个扫描代次首次自动重新捕获失败时保存关键帧。
- 最新真实帧现在输出 32–33；66–67、82–83、110–111 和窗帘样本回归保持正确，9 项自动化测试通过。

### 2026-08-26 — V3.3 大跳页自动恢复

- 用真实会话确认 90–91 跳到 110–111 时，OCR 已持续读到正确的 111，但旧页扣分使候选无法进入共识器。
- 候选评分中的旧页状态改为只提供正向偏好，不再对超过 `max_page_jump` 的可信候选额外扣分。
- 大跳页风险统一交由页面共识器控制，仍需连续三次一致，不因本次修复降低安全要求。
- 翻页后超过 `auto_reacquire_seconds` 仍未恢复时，自动进入忽略旧页偏好的重新捕获模式，无需按 `S`。
- 遥测事件新增 `reanchor` 字段，并通过 `mode=reacquire` 标记自动重新捕获扫描。
- 真实 110–111 关键帧在保留旧状态 90–91 时已能直接输出 110–111；8 项自动化测试与历史样本回归全部通过。

### 2026-08-26 — V3.2 运动迟滞与最清晰帧

- 根据首次 V3.1 会话定位到运动阈值抖动：85 次场景变化中有 42 组间隔不足 0.25 秒。
- 新增运动迟滞门：默认短窗口 5 帧中至少 3 帧有运动才开始翻页事件。
- 翻页事件会一直保持到连续安静达到 `settle_seconds`，期间的阈值起伏不会重复递增扫描代次。
- 稳定缓冲改为选择清晰度最高的帧送入 OCR，不再直接使用最后一帧。
- 界面显示运动门是否处于翻页状态，并使用 `RECOGNIZING` 表示翻页后的第一次识别。
- 新增运动门自动化测试，现有 7 项测试全部通过。

### 2026-08-26 — 确定 V3.1 开发目标

- 将下一阶段收敛为“可观测的无人值守识别验证”。
- 确定结构化日志、关键帧和界面诊断信息为下一项实现。
- 设定连续翻阅 20 次、不按 `S`、3 秒内确认和自动恢复等验收标准。
- 左右页学习、透视矫正、ESP32 通信与购买新摄像头暂缓到数据验证之后。

### 2026-08-26 — 实现 V3.1 测试观测链路

- 每次启动建立独立的 `debug/sessions/时间戳/` 测试会话。
- 使用 `events.jsonl` 记录 OCR 耗时、完整数字候选、候选排名、观察结果、处理决策和共识前后状态。
- 使用 `summary.json` 实时汇总扫描次数、无结果、异常、页面确认次数、确认延迟和人工标注准确率。
- 页面确认、新待确认候选、OCR 异常和测试标记会保存关键帧；普通帧不会持续落盘。
- 实时界面新增 OCR 距今时间、耗时、观察值、待确认进度和会话统计。
- 新增测试按键：`Y` 标记正确确认、`N` 标记错误确认、`M` 标记漏检；标记只影响测试数据，不参与识别。
- 增加会话记录器自动化测试；下一步是在手机摄像头上完成一次真实的 20 次连续翻页测试。

### 2026-08-25 — V3 持续观察

- 将一次性翻页触发改为稳定时持续异步 OCR。
- OCR 永远只处理最新稳定帧，不建立积压队列。
- 加入普通两次一致、大跳页三次一致的页面共识器。
- 加入黑屏、低对比度和低清晰度过滤。
- 最新自动调试结果改为覆盖写入 `debug/live/`，避免持续创建大量目录。
- `S` 改为重新锚定后继续收集第二次观察，不再用单帧立即确认。

### 2026-08-25 — V2 整帧 OCR

- 移除动态书本框对 OCR 输入的硬裁剪。
- 整帧识别页码候选，并按空间和上下文评分。
- 修复 66–67、82–83 代表性样本。
- `S` 改为立即捕获按键时画面并忽略旧跳页限制。

### 2026-08-24 — V1/V1.5 原型

- 建立 OpenCV 摄像头、运动检测和 RapidOCR 原型。
- 实现固定左右页 ROI、多裁剪框和多种图像预处理。
- 尝试亮色纸张轮廓跟踪，后因裁剪和误检问题退出主识别链路。

### 2026-08-29 — 云端端到端闭环、固件 0.9.2 与发布基线

- 正式链路已改为手机 HTTPS 摄像头上传最新帧、云端 RapidOCR 识别、统一状态发布和 ESP32 HTTPS 轮询，不再依赖开发电脑或同一局域网。
- 腾讯云 Web/API 与 OCR worker 分容器运行并共享持久化数据目录；实拍已识别现有书籍和页码 66–67。
- ESP32 已完成云端 LIVE、设备绑定、批注显示、反馈回传和 OTA 下载链路。
- 定位到反馈已写入服务器但屏幕显示 FEEDBACK ERROR 的原因是公网 HTTPS 响应超过原 1.5 秒等待时间；固件 0.9.2 已将相关等待窗口调整为 3/6 秒并发布 OTA，等待最终实机确认。
- 云端状态去重现在会刷新接收心跳，避免内容未变化时被误判为离线。
- 修复云端 OCR 在多个上传文件时间戳完全相同时最新帧选择不确定的问题，使用纳秒时间戳和文件名作为稳定排序键。
- README 与 PROJECT_HANDOFF 已同步真实云端架构，明确下一优先级为多用户/多设备状态隔离、识别质量验收和云端书库录入。
- 新增 requirements-dev.txt，开发环境可以显式安装 pytest；完整自动化测试 64 项通过。

### 2026-08-29 — API v11 多用户与多设备状态隔离

- 移动摄像头帧由 `user-<用户>-session-<会话>.jpg` 标识，不同阅读会话不再争用全局最新帧。
- 云端 OCR worker 可同时维护多个会话，每个会话拥有独立的封面共识、页码共识、revision 和 `relay_states/session-<会话>.json`。
- 手机网页只读取登录用户当前阅读会话的状态；ESP32 先通过设备令牌确定绑定用户，再读取该用户当前会话。
- 新会话上传首帧后自动删除该用户的旧会话帧，避免后台继续识别已结束阅读。
- 本地桌面摄像头和旧云中继仍保留全局状态兼容路径，不影响离线开发。
- Web API 升级至 v11，并新增 `session_state_isolation` 能力标识。
- 新增双用户帧文件、状态通道、worker 分组和通道身份测试；完整自动化测试 67 项通过。
