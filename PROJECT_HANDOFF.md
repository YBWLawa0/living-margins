# 众生行记（Library Terra）项目交接说明

> 更新时间：2026-08-29
> 当前阶段：手机摄像头 → 云端 OCR → 云端状态 → ESP32 → 反馈回传的端到端链路已跑通
> 线上入口：`https://nx.ybwlawa0.com/living-margins/`

## 1. 项目目标

用户用手机网页摄像头观察实体书，云端识别书籍与页码并选出对应批注，ESP32-S3 触摸屏实时显示状态，用户可以在屏幕上赞同或不赞同。正式使用不依赖开发电脑，也不要求手机和板子处于同一个局域网。

## 2. 当前架构

```text
手机 HTTPS 网页摄像头
        ↓ 限频 JPEG
腾讯云 living-margins Web/API ── SQLite、设备、批注、反馈、OTA
        ↓ 最新帧共享目录
living-margins-vision（RapidOCR + 封面匹配）
        ↓ relay_states/session-<会话>.json
Web/API 统一状态
        ↓ HTTPS 轮询
ESP32-S3-LCD-4.3
```

本地 `app.py` 摄像头识别链路仍然保留，用于算法调试和离线回归；它不是正式云端使用的前置条件。

## 3. 关键目录与文件

```text
library-terra/
├─ app.py                         # Windows 摄像头识别与现场诊断
├─ living_margins_web.py          # 账号、设备、阅读、批注、反馈、移动相机和 OTA API
├─ cloud_vision_worker.py         # 云端最新帧 OCR、封面匹配、状态发布
├─ cloud_relay.py                 # 本地识别状态向云端中继（兼容开发模式）
├─ Dockerfile                     # Web/API 容器
├─ Dockerfile.vision              # 云端 OCR 容器
├─ requirements-cloud-vision.txt  # 云端 OCR 依赖
├─ web/                           # 手机单页应用
├─ library_terra/                 # 识别、书库、状态、数据库与遥测模块
├─ firmware/
│  ├─ src/main.cpp                # LCD、触摸、网络、状态、反馈、配网和 OTA
│  ├─ include/firmware_version.h  # 当前发布版本 0.9.2
│  ├─ include/device_secrets.example.h
│  └─ publish_release.py          # 生成并发布 OTA 元数据
├─ tests/                         # 自动化测试
├─ books/                         # 本地私人书库，默认不进 Git
├─ runtime/                       # 数据库、帧、状态与 OTA 包，默认不进 Git
├─ DEVELOPMENT_LOG.md             # 按时间记录设计决策与实测证据
└─ DESIGN_RULES.md                # 前端视觉强制规则
```

## 4. 已实现且已验证

- 手机可通过正式 HTTPS 网站打开后置摄像头并持续上传最新画面。
- 云端 RapidOCR 已从实拍画面识别现有书籍和页码 66–67。
- ESP32 已完成串口刷写、触摸、中文界面、2.4 GHz Wi-Fi 配网、设备绑定和云端 `LIVE` 状态。
- 手机、云端与 ESP32 不需要处于同一网络，开发电脑可以关机。
- 批注支持草稿、灵感、提交、管理员审核和发布到屏幕。
- 赞同/不赞同记录保存在服务端；同一用户重复选择幂等，切换选择会更新原记录。
- OTA 具备版本检查、固件下载、SHA-256 完整性校验和失败保护。
- 云端容器持久化 SQLite、移动帧、书库与 OTA 文件。
- 状态具有心跳与过期保护，OCR 失联时不会长期显示历史状态为实时。
- 当前自动测试基线：67 项通过。

## 5. 当前部署

- 域名：`https://nx.ybwlawa0.com/living-margins/`
- 主机：腾讯云 Ubuntu，公网 IP `81.70.232.81`
- Web 容器：`living-margins`，镜像阶段 `living-margins-cloud:0.4`
- OCR 容器：`living-margins-vision`，镜像阶段 `living-margins-vision:0.3`
- 数据目录：服务器 `/home/ubuntu/living-margins/data` 挂载至容器 `/data`
- Web 仅绑定主机 `127.0.0.1:18780`，由现有 HTTPS 反向代理暴露公网入口。

部署凭据、设备令牌、Wi-Fi 密码和 `.env` 不进入 Git。接手人应从安全渠道单独取得，并避免在日志或截图中公开。

## 6. 待实机确认

- 固件 0.9.2 已构建并发布 OTA，用于把反馈请求等待时间从 1.5 秒提高到适合公网 HTTPS 的范围。
- 服务器已确认此前“页码识别成功但屏幕 FEEDBACK ERROR”时反馈实际上成功写入；因此问题是设备过早超时，不是数据层失败。
- 下一次拿到设备时只需确认：升级页面显示 0.9.2，随后连续执行“赞同 → 不赞同 → 赞同”，屏幕与网页均反映最终选择且不再报错。

## 7. 下一阶段开发计划

### P0：形成可复现发布基线

- 更新 README、交接文档与开发日志，提交并推送当前全部代码。
- 保存 64 项测试结果和线上健康检查结果。
- 为云端部署补充可重复执行的启动/升级说明，避免依赖命令历史。

### P1：多用户和多设备状态隔离（已部署，待并发实机验收）

API v11 已按阅读会话隔离移动帧、OCR 共识和状态文件，网页按登录用户当前会话读取，ESP32 按设备绑定用户读取。

- [x] 移动帧按用户/阅读会话保存最新帧。
- [x] OCR 状态按阅读会话通道发布。
- [x] 网页与屏幕轮询只读取所属用户的当前会话通道。
- [x] 增加两个用户并发上传、状态互不串台的自动测试。
- [x] Web 0.4 与 OCR 0.3 已部署，公网 API v11 健康检查通过。
- [ ] 使用两部手机和两台设备完成云端并发现场验收。

### P1：识别质量验收

- 用至少 3 本书、20 次连续翻页和多种光照/距离建立现场测试表。
- 记录书籍识别率、页码正确率、漏检率和确认延迟。
- 改进反光、倾斜、小页码、175/75 等混淆场景。
- 增加封面/页码取景提示和“当前画面为何未识别”的用户提示。

### P1：书库云端录入

当前云端只有已手工迁移的一本书。需要在手机网页实现拍封面、OCR 建议书名、人工确认和上传参考封面的完整录入流程，并提供书籍与批注管理页面。

### P2：生产安全与运维

- 用签名固件替代只有 SHA-256 的 OTA 完整性校验。
- 显式初始化管理员，移除“第一个账号自动成为管理员”的 Demo 规则。
- 增加登录限速、CSRF/CORS 策略、审计日志、数据库迁移、备份与恢复。
- 校验 ESP32 TLS 证书，不使用不验证证书的连接方式。
- 加入健康检查、结构化日志、告警、版本页和回滚说明。

### P2：固件可维护性

- 将 `firmware/src/main.cpp` 拆成网络、状态、UI、配网、反馈和 OTA 模块。
- 统一网页与设备错误码，并在屏幕展示可行动的错误原因。
- 建立 API/schema 与固件版本兼容矩阵。

## 8. 建议接手顺序

1. 阅读本文件、`DESIGN_RULES.md` 和 `DEVELOPMENT_LOG.md`。
2. 运行完整测试并检查线上 `/api/health`。
3. 用设备完成 0.9.2 OTA 与反馈切换验收。
4. 使用两个账号并发拍摄，完成多用户状态隔离的线上验收。
5. 同时积累 20 次以上现场识别数据，再针对证据调整 OCR。

## 9. 交接验收清单

- [x] 手机 HTTPS 网页可调用摄像头并把画面送达云端。
- [x] 云端可识别已迁移书籍与页码。
- [x] ESP32 可联网、绑定并显示云端 `LIVE` 状态。
- [x] 电脑关机后，手机、云端和 ESP32 主链路仍可工作。
- [x] 手机可提交批注，管理员可审核，屏幕可显示已批准批注。
- [x] 服务端赞同/不赞同记录支持幂等与切换。
- [x] OTA 检查、下载与完整性校验链路可用。
- [x] Git 忽略设备密钥、Wi-Fi 密码、数据库、帧、日志和书库私有数据。
- [ ] ESP32 0.9.2 实机升级后反馈不再误报超时。
- [x] 自动化验证两个用户同时阅读时状态完全隔离。
- [ ] 两部手机/两台设备完成云端并发现场验收。
- [ ] 完成至少 3 本书、20 次翻页的量化现场验收。
