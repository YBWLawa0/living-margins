# Living Margins UI Rule

> 本文件是众生行记前端的强制视觉规范。新增或修改任何网页界面时，必须先满足本文件，再考虑局部创意。

设计基线来自 StudioBlank：界面应退后，让书籍、页码和批注成为主体。白空间本身就是主要设计元素。

## 1. 核心原则

1. 只使用白底、黑字和灰色分隔线建立层级。
2. 使用严格栅格、字体字重和留白，不依赖装饰建立“设计感”。
3. 所有表面保持平面、几何和克制。
4. 页面首先服务阅读状态和操作，不制造与功能无关的视觉焦点。
5. 中文产品文案保持简短；辅助状态使用英文大写或等宽字体。

## 2. 强制颜色

- 主文字与主要操作：`#0A0A0A`
- 页面背景：`#FAFAFA`
- 内容表面：`#FFFFFF`
- 分隔线：`#D4D4D8`
- 弱分隔与悬停底色：`#F4F4F5`
- 次要文字：`#71717A`
- 禁用文字：`#A1A1AA`
- 成功状态：`#16A34A`
- 错误状态：`#DC2626`

成功色和错误色只用于真实状态反馈，不得作为品牌装饰色。不得新增其他强调色。

## 3. 字体

- 标题与正文：Inter；中文回退到 Noto Sans SC、Microsoft YaHei、系统无衬线字体。
- 状态、编号、机器码：IBM Plex Mono；回退到系统等宽字体。
- 大标题：40–64px，700，紧凑字距与 1.05–1.1 行高。
- 二级标题：28px，600。
- 正文：16px，300–400，1.65–1.8 行高。
- 标签与状态：12–13px，等宽，大写，0.05em 字距。

层级优先通过 300、400、600、700 的字重差异建立，避免无限放大字号。

## 4. 间距与栅格

- 基础单位：16px。
- 紧密间距：8px。
- 组件间距：16px。
- 组内间距：32px。
- 区块内边距：48px。
- 主要区块间距：64–96px。
- 桌面页边距：48px；移动端页边距：20px。
- 内容最大宽度：1120px。

任何主要区块之间不得少于 64px。宁可增加留白，也不要加入装饰填补空白。

## 5. 组件规则

### 按钮

- 主按钮：黑底白字，1px 黑色边界。
- 次按钮：白底黑字，1px 黑色边界。
- 悬停时只做黑白反转，不使用位移、缩放和长动画。
- 大按钮高度 48px，中按钮 40px。

### 输入框

- 白底，1px 灰线，48px 高。
- 聚焦使用 2px 黑线和 2px 外偏移。
- 不使用内阴影、发光和圆角。

### 列表与卡片

- 优先使用由水平线分隔的记录列表。
- 必须使用卡片时：白底、1px 灰线、零圆角、零阴影。
- 状态放在文字列或矩形标签中，不用装饰性图标表达。

### 弹窗

- 白底、1px 黑线、直角。
- 桌面居中，移动端贴底。
- 背景只允许白色半透明遮罩，不使用模糊、阴影和渐变。

## 6. 明确禁止

- 任何非零圆角。
- 任何阴影、发光、玻璃拟态或立体效果。
- 任何渐变、噪点、纹理、装饰图案。
- 圆形内放置大字、字母或无意义符号的“伪图标”。
- 与功能无关的轨道、光圈、波纹、漂浮形状。
- 多彩品牌色或超过一个强调色。
- 超过 200ms 的动画；默认应无动画。
- 用 Emoji 充当正式产品图标。
- 在旧视觉上局部换色；方向变化时应直接删除旧组件。

## 7. Living Margins 页面映射

- 登录页：左侧价值主张，右侧账号操作；移动端改为上下结构。
- 首页：一个明确的“开始阅读”主操作，设备和灵感使用编号列表。
- 阅读页：页码是唯一视觉主角，书名、识别状态和设备信息作为次级文本。
- 绑定屏幕：机器码输入是唯一任务，不加入插画或配对动画。
- 后续批注页：文本编辑器优先，状态使用 Draft、Pending、Approved、Rejected 的直角标签。

## 8. 提交前检查

- 搜索 CSS，确认没有非零 `border-radius`、有效 `box-shadow` 或 `gradient`。
- 确认没有装饰性 Emoji、圆形大字和无功能图形。
- 桌面与 320px 宽度下都能完成主要操作。
- 所有按钮和输入框有可见键盘焦点。
- 状态不能只依赖颜色表达。
- 页面保留至少 64px 的主要区块间距。
- 文案、页码和批注始终比界面装饰更醒目。
